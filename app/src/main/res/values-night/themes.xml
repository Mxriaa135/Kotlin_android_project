<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppPostagens" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/backgroundAppDark</item>
        <item name="colorPrimary">@color/backgroundAppDark</item>
        <item name="colorPrimaryDark">?attr/android:colorBackground</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorAccent">@color/white</item>
    </style>
</resources>